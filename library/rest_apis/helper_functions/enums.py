# Dictionary defining URL values for constructing book API URLs
url_values = {
    "url": "https://gutendex.com/books/",
    "page_parameter": "page",
    "search_parameter": "search"
}

# Max pending balance a member can have before
max_pending_balance = 500